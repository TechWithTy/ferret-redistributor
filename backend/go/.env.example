SITEMAP_URL=https://www.dealscale.io/sitemap.xml
RSS_FEED_URL=https://rss.beehiiv.com/feeds/th0QQipR7J.xml
DATABASE_URL=postgres://user:pass@localhost:5432/postiz?sslmode=disable

# Valkey/Redis cache
VALKEY_ADDR=127.0.0.1:6379
# VALKEY_PASSWORD=
VALKEY_DB=0

# API
API_PORT=8080

# JWT (optional; if set, API will issue/accept JWT instead of opaque sessions)
# JWT_SECRET=change-me
# JWT_ISSUER=ferret
# JWT_TTL=24h

# OpenTelemetry (optional; enable with build tag `otel`)
# OTEL_ENABLED=1
# OTEL_SERVICE_NAME=ferret
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# Pulsar (optional; default stub unless built with -tags=pulsar)
# PULSAR_SERVICE_URL=pulsar://localhost:6650
# PULSAR_TOKEN=
# PULSAR_TOPIC_POST_EVENTS=persistent://public/default/post-events
# PULSAR_TOPIC_SCHEDULE_EVENTS=persistent://public/default/schedule-events
