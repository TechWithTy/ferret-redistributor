# Local-only environment variables (DO NOT COMMIT a real `.env`)
#
# Copy this file to `.env` and fill values, or export them in your shell.

## --- GroupMe bot (local harness: go/cmd/groupmebot) ---
# Shared secret for inbound callbacks (required). Provide as:
# - query:  https://<tunnel-host>/webhooks/groupme?token=<GROUPME_WEBHOOK_TOKEN>
# - header: X-Webhook-Token: <GROUPME_WEBHOOK_TOKEN>
GROUPME_WEBHOOK_TOKEN=

# Bot id used to post messages back to GroupMe (required for outbound).
GROUPME_BOT_ID=

# Optional: override API base URL and port.
GROUPME_BASE_URL=https://api.groupme.com/v3
GROUPME_PORT=8081

# Optional (not used by the harness today; useful for future bot management).
GROUPME_ACCESS_TOKEN=
GROUPME_GROUP_ID=

## --- GroupMe sync (go/cmd/groupmesync) ---
# Used to list groups and bots (GroupMe "user" access token).
# (Your repo may already use GROUP_ME_MASTER_TOKEN; groupmesync supports either.)
GROUPME_ACCESS_TOKEN=
GROUP_ME_MASTER_TOKEN=

## --- Notion (go/cmd/groupmesync) ---
# Notion integration token (new tokens may start with ntn_; treat as opaque).
NOTION_API_KEY=

# Data Source IDs (collection://...) from your Notion DB Stack (preferred since 2025-09-03).
NOTION_DATA_SOURCE_ID_GROUPS=
NOTION_DATA_SOURCE_ID_BOTS=

## --- Go API server (go/cmd/api) ---
API_PORT=8080

## --- Database (Go API) ---
DATABASE_URL=postgres://user:pass@localhost:5432/dbname?sslmode=disable
DB_MAX_OPEN_CONNS=10
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=30m

## --- Auth (Go API) ---
JWT_SECRET=
JWT_TTL=24h
JWT_ISSUER=

## --- Telemetry (optional) ---
OTEL_ENABLED=0
OTEL_SERVICE_NAME=
OTEL_EXPORTER_OTLP_ENDPOINT=


